pre-commit:
  parallel: true
  commands:
    lint:
      run: yarn eslint {staged_files}
    types:
      run: yarn tsc --noEmit
commit-msg:
  parallel: true
  commands:
    commitlint:
      run: yarn commitlint --edit
